-- --------------------------------------------------------------------------
-- (C) Copyright IBM Corporation 2017
-- All Rights Reserved
-- --------------------------------------------------------------------------


-- Cameras
the camera 'Front Room Door Camera'
  has '32.5' as x position and
  has '37.5' as y position and
  has '0' as orientation.

the camera 'Front Room Window Camera'
  has '25' as x position and
  has '42.5' as y position and
  has '180' as orientation.

the camera 'Bedroom Door Camera'
  has '47.5' as x position and
  has '25' as y position and
  has '270' as orientation.

the camera 'Bedroom Window Camera'
  has '72.5' as x position and
  has '25' as y position and
  has '90' as orientation.


-- Temperature sensors
the temperature sensor 'Front Room Temp Sensor'
  has '12' as x position and
  has '32' as y position and
  has '0' as orientation.

the temperature sensor 'Bedroom Temp Sensor'
  has '42' as x position and
  has '2' as y position and
  has '0' as orientation.


-- Lights
the binary light 'Front overhead'
  has '25' as x position and
  has '45' as y position and
  has '0' as orientation.

the binary light 'Back overhead'
  has '25' as x position and
  has '35' as y position and
  has '0' as orientation.

the binary light 'Side lamp'
  has '35' as x position and
  has '45' as y position and
  has '0' as orientation.

the binary light 'Hallway overhead'
  has '25' as x position and
  has '25' as y position and
  has '0' as orientation.

the binary light 'Bathroom overhead'
  has '30' as x position and
  has '10' as y position and
  has '0' as orientation.

the binary light 'Bedroom overhead'
  has '60' as x position and
  has '20' as y position and
  has '0' as orientation.

the binary light 'Bedroom lamp'
  has '70' as x position and
  has '10' as y position and
  has '0' as orientation.


-- Door sensors
the door sensor 'Front Door Sensor'
  has '100' as x position and
  has '100' as y position and
  has '0' as orientation.

the door sensor 'Front-to-Hallway Door Sensor'
  has '100' as x position and
  has '100' as y position and
  has '0' as orientation.

the door sensor 'Bathroom-to-Hallway Door Sensor'
  has '100' as x position and
  has '100' as y position and
  has '0' as orientation.

the door sensor 'Bedroom-to-Hallway Door Sensor'
  has '100' as x position and
  has '100' as y position and
  has '0' as orientation.


-- Window sensors
the window sensor 'Front Left Window Sensor'
  has '100' as x position and
  has '100' as y position and
  has '0' as orientation.

the window sensor 'Front Middle Window Sensor'
  has '100' as x position and
  has '100' as y position and
  has '0' as orientation.

the window sensor 'Front Right Window Sensor'
  has '100' as x position and
  has '100' as y position and
  has '0' as orientation.

the window sensor 'Bathroom Window Sensor'
  has '100' as x position and
  has '100' as y position and
  has '0' as orientation.

the window sensor 'Bedroom Window Sensor'
  has '100' as x position and
  has '100' as y position and
  has '0' as orientation.
